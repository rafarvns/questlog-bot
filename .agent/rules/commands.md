# Terminal and Package Manager Rules

- Always utilize `pnpm` for all package management tasks. Do not use `npm` or `yarn`.
- Exclusively use `cmd` for executing terminal commands. Do not use `powershell`, `bash`, or other shell environments.