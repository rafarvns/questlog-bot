# --- Shared Project Variables ---
# Postgres Configuration
POSTGRES_USER=questlog
POSTGRES_PASSWORD=questlog_pass
POSTGRES_DB_CRAIG=questlog
POSTGRES_DB_SCRIBBLE=scribble_db

# --- Craig Service Variables ---
# Discord API keys (Fill these!)
DISCORD_BOT_TOKEN=your_token_here
DISCORD_APP_ID=your_app_id_here
DISCORD_CLIENT_SECRET=your_client_secret_here
DEVELOPMENT_GUILD_ID=your_guild_id_here

# Internal connection strings for Craig (using Docker service names)
REDIS_URL=redis://redis:6379

# --- Scribble Scribe Variables ---
# Internal connection strings for Scribble
# Note: For external access from your host, use port 5435 (Postgres) and 6385 (Redis)

# Whisper configuration
# Available models: tiny, base, small, medium, large (small or medium recommended for accuracy vs speed)
WHISPER_MODEL=small
# Available devices: cpu, cuda (cuda requires NVIDIA GPU and drivers)
WHISPER_DEVICE=cpu
# This corresponds to the internal path in the container
RECORDINGS_PATH=/recordings
