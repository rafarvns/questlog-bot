[project]
name = "scribble-scribe"
version = "0.1.0"
description = "Transcription service for Questlog Bot"
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[project.scripts]
scribble = "main:main"

[tool.setuptools]
py-modules = ["main"]
