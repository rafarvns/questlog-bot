# Database configuration
DATABASE_URL=postgresql://user:password@localhost:5432/questlog_scribble

# Whisper configuration
# Options: tiny, base, small, medium, large (small or medium recommended)
WHISPER_MODEL=base
# Options: cpu, cuda (cuda needs NVIDIA GPU)
WHISPER_DEVICE=cpu

# Craig recordings path
RECORDINGS_PATH=../../services/craig/recordings
